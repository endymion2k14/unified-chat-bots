# Unified Chat Bots

# Oauth

## Tries to implement:
 - Oauth tokenflow on /oauth/callback
 - Initial tokenflow start /oauth/twitch/bot/{botindex} for bot tokens
 - Initial tokenflow start /oauth/twitch/broadcaster/{botindex} for broadcaster tokens
 - Bot starts with appToken still, which is 60 days. (fallback purpose)
 - Variable 'botToken' can be used for bot Oauth tokenflow
 - Variable 'broadcasterToken' can be used for broadcaster Oauth tokenflow
 - Scopes editable from the json
 - Updated corresponding example files
